# EHC 案件 引き継ぎメモ（新規Cowork用・2026-07-26）

作成: 2026-07-26 / 引き継ぎ元セッション → 新規Cowork
このファイルの内容を新規Coworkの最初のメッセージに貼れば、そのまま続きから作業できます。
Obsidianに入れる場合は `99_HANDOFF/EHC_引き継ぎ_2026-07-26.md` として保存推奨（※今セッションはObsidian MCPが切断中のため直接書き込めず、このファイルで代替）。

---

## 0. 前提（誰の・何の案件か）

- ユーザー: SHINICHI MOROZUMI（合同会社 nene / info@neneweb.com / GitHub: Shinichi-Morozumi）
- 事業: 株式会社EHCソリューションズ＝業務用空調のGX／炭化水素冷媒「ドロップイン」＋補助金マッチング
- 対応スタイル: 日本語・簡潔・冗長な説明は不要
- 作業フォルダ: `~/Desktop/A8_EHC_GX炭化水素冷媒/ehc-subsidy-app-new`（＝Gitリポジトリ ehc-subsidy-app 本体）
- レビュアー: **桝口さん（ドロップイン担当）** / **碓井さん（更新工事担当）**
  - ※関連ファイル `vr1_2026-07_app_check_usui_masuguchi.xlsx` あり（アプリ内容チェック用）

---

## 1. このセッションで完了したこと

### ① 送信APIにNotion自動追記機能 → 本番稼働中
- 提案PDFをメール送信するたびに、Notion「EHC見込み顧客アタックリスト」にステータス「提案送付済み」で1行自動追加される。
- 実装: `app/api/send-proposal/route.ts` の `appendLeadToNotion(lead)`（メール送信成功時に呼ぶ）。best-effort try/catch、NOTION_TOKEN未設定ならno-op、8秒タイムアウト。
- コミット `3b31946`（本番反映済み・READY確認済み）。
- 書き込みフィールド: 会社名(title), ステータス, 補助金額(概算), 回収年数, 希望制度, 担当メール, 電話, 住所, 提案No, 送信日, メモ。
- Notion: API version 2022-06-28 / data source `collection://b6fd594e-c443-430a-b4c2-06ce18c4472e` / NOTION_DB_ID 既定 `27c3f8fe-bc9b-49f7-bba1-430b90697cec`。

### ② 本番メール設定のリストア（テスト後の後始末）完了
- E2Eテスト用に info@neneweb.com へ寄せていた env var 2つ（`PROPOSAL_TO_EMAIL` / `PROPOSAL_CC_EMAIL`）を **削除** → コードのハードコード既定値が有効化。
- 現在の本番宛先: **To: info@ehcjpn.com / Cc: info@project-neo.co.jp**（route.ts の既定fallback）。
- 本番を再デプロイ（build cache off）して Ready 確認済み。
- ※Vercelのenv varはビルド時にバイクインするため、変更後は必ず再デプロイが必要。Sensitive varは読み戻せないので「削除して既定値を正とする」方式でリストアした。

### ③ 営業ロードマップ（4軸）の壁打ち・作戦会議
実データ（vault顧客MOC＋Web調査）に基づく。中核の戦略洞察:
> **システムが「提案作成」を自動化した結果、ボトルネックは入口（リード獲得）と出口（フォロー→クロージング）の2つに移った。**

**4軸:**
1. **軸1 既存リスト深耕**（停滞パイプラインのリバイバル）← 最短リターン
2. **軸2 新規開拓**（入口を太らせる・中長期）
3. **軸3 代理店・パートナー**（他人の足で入口を増やす・中長期）
4. **軸4 補助金公募連動**（公募カレンダーに営業を同期）

**最優先の結論: 「軸1（停滞リバイバル）＋軸4（公募一斉アプローチ）を8月に集中砲火」** が一番リターンが早い。9月の3次公募→申請ラッシュに間に合わせるには8月が逆算のラストチャンス。

### ④ 「ここを攻める」図（attack map）作成済み
5段階ファネル（①リード獲得 → ②マッチング＋ROI → ③PDF提案自動送信 → ④フォロー/現地調査【出口の壁・強調】 → ⑤見積→補助金申請→受注）。軸2/軸3が左の入口、軸1が右のフォロー段（今すぐ攻める）、下部に8月中心の補助金タイムライン。
- ※チャット内SVGとして表示済み。画像ファイルとしては未保存。新Coworkで再生成する場合は visualize MCP（show_widget）で「ehc_sales_roadmap_attack_map」を再作成。

### ⑤ 数字の検証（外部ファクト）完了
調査エージェントでSII/経産省/環境省を裏取り:
- 補助率: 設備単位型 **1/3**、GX設備単位型 最上位区分 **1/2**、工場・事業場型 先進枠 **2/3** → 手元一致
- 上限: 設備単位型 **1事業1億円**（下限300万）／GX設備単位型 **3億円**／工場・事業場型 15億円規模 → 案件規模で狙う枠が変わる
- 2026公募: 1次 3/30–4/27、2次 6/1–**7/9締切（終了済み）**、**3次は日程未定（SII公表待ち）** ← ★「9月上旬」は裏取れず。資料は「日程未定」に修正すること
- フロン規制: HFC段階削減 2029年▲70%（規制厳格化）／GWP目標 2030年頃約450／R22は2020年生産全廃 → すべて一致

---

## 2. アプリの現状（ehc-subsidy-app）

- 名称: 「EHC 補助金マッチング & ROI 即答ツール」
- 技術: Next.js 14.2.18（App Router）+ TypeScript + React 18 + Tailwind + Vercel（Hobby）
- 本番URL: **https://ehc-subsidy-app.vercel.app**（noindex＝URL限定共有）
- リポジトリ: Shinichi-Morozumi/ehc-subsidy-app
- 状態: Notion自動追記まで本番稼働。メール宛先は本番値にリストア済み。

---

## 3. 次にやること（＝引き継ぐタスク本体）

### A. 営業ロードマップの深掘り（ユーザーが選ぶ・未着手）
1. **8月一斉アプローチの文面・対象セグメント設計**（誰に何を送るか）
2. **Notionステージ＆自動フォローを実際に組む**（出口の壁を仕組みで埋める）
3. **代理店スキーム（紹介料テーブル）を詰める**（入口を他人の足で増やす）

### B. 数字の確定作業
- **社内パイプライン数字はユーザー確認待ち**（Obsidian/Notionが今切断のため再照合できていない）。下記を最新値と突き合わせる:
  - クロスドック晴海 約991万（押印待ち）／メガフェイス3店 8,400万規模／徳真会 1,976万（完了）／全体 約23社・約1.35億円
  - ※上3件だけで約1.14億、残り約2,000万強は小口の積み上げという理解。要確認。
- 資料の「3次公募 9月上旬」→「**3次は日程未定（SII公表待ち）**」に修正。
- 大型案件（メガフェイス等）は**GX設備単位型（上限3億）**での試算余地あり。

### C. 保存作業（未完）
- **この作戦会議の要点をObsidianに保存する**（方向性が固まったら）。MCP復帰後に `99_HANDOFF/` と `01_Projects/EHC_Solutions/` へ。

---

## 4. ユーザー側の手作業（残）

- **Notionのテスト行を削除**: 提案No `TEST-1784879888838` / 会社名「【テスト】Notion自動追記 (削除OK)」（永久削除は本人操作）。

---

## 5. 主要パス・数値の早見表

| 項目 | 値 |
|---|---|
| 作業フォルダ | `~/Desktop/A8_EHC_GX炭化水素冷媒/ehc-subsidy-app-new` |
| 本番URL | https://ehc-subsidy-app.vercel.app（noindex） |
| Notion自動追記 実装 | `app/api/send-proposal/route.ts` の `appendLeadToNotion()` |
| Notion自動追記 commit | `3b31946`（本番READY） |
| 本番メール宛先 | To: info@ehcjpn.com / Cc: info@project-neo.co.jp（route.ts既定） |
| NOTION_DB_ID 既定 | `27c3f8fe-bc9b-49f7-bba1-430b90697cec` |
| Notion data source | `collection://b6fd594e-c443-430a-b4c2-06ce18c4472e` |
| 補助率 | 設備単位型1/3・GX最上位1/2・工場先進枠2/3 |
| 補助上限 | 設備単位型1億／GX 3億／工場15億規模 |
| 2026公募 | 1次3/30–4/27・2次6/1–7/9(終了)・**3次 日程未定** |
| パイプライン(要確認) | 約23社・約1.35億円 |

---

## 6. 技術メモ（環境固有・ハマりどころ）

- Vercel env varはビルド時バイクイン → 変更後は必ず再デプロイ。
- Sensitive varは読み戻し不可 → リストアは「削除して既定値を正にする」。
- ビルドは/tmpコピー方式（マウント先.nextはEPERM）: tar除外コピー＋node_modulesシンボリックリンク→`npx next build`。
- pushはGitHub Desktop（computer-use）経由。他Claudeセッション占有時は待って再試行。
- 本番検証: サーバー側文字列は `curl -s <URL> | grep`、クライアント側は page chunk js を grep。
- ブラウザ（Chrome）は computer-use では "read" tier → 操作は Chrome MCP（mcp__Claude_in_Chrome__*）で。

---

*このメモを新規Coworkに貼れば続きから再開できます。Obsidian復帰後は `99_HANDOFF/EHC_引き継ぎ_2026-07-26.md` として保存を。*
